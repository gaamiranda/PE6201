# ─── Secrets. Never commit these: the repository is public. ──────────────────
.env
.env.*
!.env.example
*.key
secrets.*
**/openrouter_key*

# ─── Teaching workspace (personal study material, not part of the submission) ─
MISSION.md
NOTES.md
RESOURCES.md
lessons/
reference/
assets/
learning-records/
.claude/
.agents/
skills-lock.json
# ─── Course materials issued by the instructor (not ours to republish) ───────
# Named individually on purpose: a blanket *.pdf would also swallow our own report.
PE6201_A2_Applied_AI_System.pdf
PE6201_A2_FAQ.pdf
PE6201_A2_Document_Updates.pdf
PE6201_A2_Team_Self_Appraisal.docx
TEAM_DECLARATION.docx
~$AM_DECLARATION.docx
# ─── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# ─── Editors and OS ──────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp

# Run output: the raw decision log is regenerated on every run and would churn every diff.
# The RESULT TABLES in results/ are committed on purpose — the brief requires them in the repo.
results/decisions.jsonl

# Scratch decision logs written by evaluation/check_scripted_replay.py during its two passes.
# The check exists to prove results/decisions.jsonl is NOT touched; its own scratch is not evidence.
evaluation/step4_isolated_results/
